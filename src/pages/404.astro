---
import Layout from "../layouts/MainLayout.astro";
import { getLocaleFromUrl, path, useTranslations } from "../i18n/ui";

const lang = getLocaleFromUrl(Astro.url);
const t = useTranslations(lang);
---

<Layout title={t("404.titulo")} description={t("404.texto")}>
  <section class="section">
    <div class="container text-center py-20">
      <p class="text-8xl font-bold text-gradient">404</p>
      <h1 class="mt-4">{t("404.titulo")}</h1>
      <p class="lead">{t("404.texto")}</p>
      <a
        href={path(lang)}
        class="btn-brand btn-lg mt-8"
        >{t("nav.inicio")}</a
      >
    </div>
  </section>
</Layout>
